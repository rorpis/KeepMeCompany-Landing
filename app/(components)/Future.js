import React from 'react';
import styles from '../(styles)/Future.module.css';

const Future = () => {
  return (
    <div className={styles.future}>
      <p className={styles.mainText}>The future is today,</p>
      <p className={styles.subText}>and your parent gets to be a part of it too.</p>
    </div>
  );
};

export default Future;